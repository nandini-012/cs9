# LandingPage

Public home page. Renders the `Navbar` and a hero section with feature cards. Opens `LoginModal` as an overlay.

## Route

`/` (public)

## Usage

```tsx
import { LandingPage } from '@/pages/landing'
```

## Related

- `LoginModal` — called from here as an overlay
- `Navbar` — rendered at the top