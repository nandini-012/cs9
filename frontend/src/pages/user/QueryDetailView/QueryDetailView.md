# QueryDetailView

Question detail page with threaded comments, resolution stepper, leaderboard widget, and inline reply box.

## Route

`/query/:id` (authenticated)

## Usage

```tsx
import { QueryDetailView } from '@/pages/user'
```