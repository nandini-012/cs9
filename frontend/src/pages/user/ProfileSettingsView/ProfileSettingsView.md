# ProfileSettingsView

Student profile editing page. Includes avatar, personal info, academic info (roll no read-only), and security section.

## Route

`/profile` (student)

## Usage

```tsx
import { ProfileSettingsView } from '@/pages/user'
```