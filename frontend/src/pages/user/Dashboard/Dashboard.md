# Dashboard

Minimal student dashboard — search bar + raise query CTA + empty state for their queries list.

## Route

`/dashboard` (student)

## Usage

```tsx
import { Dashboard } from '@/pages/user'
```

Note: `StudentDashboard` is the fuller v2 version with sidebar nav, activity feed, and top contributors widget.