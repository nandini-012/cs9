# QueriesManagementView

Admin query management workspace — inbox list on the left, thread + resolution panel on the right. Supports internal notes, FAQ promotion, approval escalation, and resolution.

## Route

`/admin/queries` (admin)

## Usage

```tsx
import { QueriesManagementView } from '@/pages/admin'
```