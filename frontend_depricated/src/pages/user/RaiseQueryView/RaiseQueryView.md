# RaiseQueryView

Alternative query submission form with drag-and-drop file upload and similar query suggestions.

## Route

`/raise-query` (authenticated)

## Usage

```tsx
import { RaiseQueryView } from '@/pages/user'
```

Note: `RaiseQuery` (the other version) uses a simpler upload box without drag-and-drop. Both are valid.