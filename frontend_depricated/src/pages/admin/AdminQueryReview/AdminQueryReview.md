# AdminQueryReview

Individual query review page for admins — read query details, add resolution notes, and take action (flag, escalate, or resolve).

## Route

`/admin/review/:id` (admin)

## Usage

```tsx
import { AdminQueryReview } from '@/pages/admin'
```